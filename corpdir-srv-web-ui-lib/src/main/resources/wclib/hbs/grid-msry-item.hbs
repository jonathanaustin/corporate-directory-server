<div class="item span-{{spans}} {{itemClasses}}" id="{{itemId}}">
    <div class="item-content" id="{{itemId}}-content">
      {{content}}
      {{#if resize}}
         <script type='text/javascript'>require(['wclib/js/wc-grid-msry-resize'], function(fit){fit('{{gridId}}');});</script>
      {{/if}}
    </div>
</div>